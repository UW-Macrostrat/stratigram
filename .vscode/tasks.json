{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run backend",
      "type": "shell",
      "command": "poetry run stratiform up",
      "options": {
        "cwd": "${workspaceRoot}/manager"
      },
      "problemMatcher": []
    },
    {
      "label": "Sync schema",
      "type": "shell",
      "command": "poetry run stratiform sync",
      "options": {
        "cwd": "${workspaceRoot}/manager"
      },
      "problemMatcher": []
    }
  ]
}
